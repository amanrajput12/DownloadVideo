services:
  - type: web
    name: youtube-downloader
    env: node
    buildCommand: |
      apt-get update
      apt-get install -y ffmpeg python3
      npm install -g yt-dlp
      npm install
    startCommand: npm start
